<div class="sidebar-common">

    <div class="sidebar-top">
      <div class="sidebar-logo text-center">
        <%= link_to root_path do %>
          <%= image_tag "logoblanc" %>
        <% end %>
      </div>
      <div class="tools">
        <% if user_signed_in? %>
          <div class="sidebar-top-except-add">
            <div class="tab-button-add">
              <%= link_to company_path(@company, anchor: "job_offers") do %>
                <div class="tabs <%= set_main_category("companies", params[:controller]) %>">
                  <h4><i class="fa fa-cog"></i> My Dashboard </h4>
                </div>
              <% end %>
            </div>
            <div class="tab-button-add">
              <%= link_to company_candidates_path(@company) do %>
                <div class="tabs <%= set_main_category("candidates", params[:controller]) %>">
                  <h4><i class="fa fa-users"></i> My Matching </h4>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <div class="logout-button">
      <p><i class="fa fa-sign-out"></i><%= link_to "  LOGOUT", destroy_user_session_path, method: :delete %></p>
    </div>

</div>
