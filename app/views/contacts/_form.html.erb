<%= simple_form_for (@contact) do |f| %>
  <%= f.error_notification %>

  <%= f.input :email %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :message %>
  <%= f.submit 'Contact me', class: "btn btn-primary"%>
<% end %>
