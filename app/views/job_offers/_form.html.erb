<%= simple_form_for ([@company, job_offer]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :title %>
  <%= f.input :document, as: :attachinary, label: "Click HERE to Upload a Job Offer or Drag it HERE" %>
  <%= f.button :submit, class: "btn btn-primary"%>
<% end %>
