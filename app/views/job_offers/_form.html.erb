<%= simple_form_for ([@company, job_offer]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :title %>
  <%= f.input :document, as: :attachinary, label: "DRAG & DROP or CLICK to add your job offer" %>
  <%= f.button :submit, class: "btn btn-primary"%>
<% end %>
